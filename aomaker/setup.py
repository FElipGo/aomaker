from setuptools import setup, find_packages

setup(
    name='aomaker',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # 项目的依赖库，例如
        # 'requests', 'numpy',
    ],
)
